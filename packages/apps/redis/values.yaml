##
## @section Common parameters
##

## @typedef {struct} Resources - Explicit CPU and memory configuration for each Redis replica.
## @field {quantity} [cpu] - CPU available to each replica.
## @field {quantity} [memory] - Memory (RAM) available to each replica.

## @enum {string} ResourcesPreset - Default sizing preset.
## @value t1.nano
## @value t1.micro
## @value t1.small
## @value t1.medium
## @value t1.large
## @value t1.xlarge
## @value t1.2xlarge
## @value t1.4xlarge
## @value c1.nano
## @value c1.micro
## @value c1.small
## @value c1.medium
## @value c1.large
## @value c1.xlarge
## @value c1.2xlarge
## @value c1.4xlarge
## @value s1.nano
## @value s1.micro
## @value s1.small
## @value s1.medium
## @value s1.large
## @value s1.xlarge
## @value s1.2xlarge
## @value s1.4xlarge
## @value u1.nano
## @value u1.micro
## @value u1.small
## @value u1.medium
## @value u1.large
## @value u1.xlarge
## @value u1.2xlarge
## @value u1.4xlarge
## @value m1.nano
## @value m1.micro
## @value m1.small
## @value m1.medium
## @value m1.large
## @value m1.xlarge
## @value m1.2xlarge
## @value m1.4xlarge
## @value nano
## @value micro
## @value small
## @value medium
## @value large
## @value xlarge
## @value 2xlarge

## @param {int} replicas - Number of Redis replicas.
replicas: 2

## @param {Resources} [resources] - Explicit CPU and memory configuration for each Redis replica. When omitted, the preset defined in `resourcesPreset` is applied.
resources: {}

## @param {ResourcesPreset} resourcesPreset="t1.nano" - Default sizing preset used when `resources` is omitted.
resourcesPreset: "t1.nano"

## @param {quantity} size - Persistent Volume Claim size available for application data.
size: 1Gi

## @param {string} storageClass - StorageClass used to store the data.
## @immutable
## @x-cozystack-options {source: storageclass}
storageClass: ""

## @param {bool} external - Enable external access from outside the cluster.
external: false

## @enum {string} Version
## @value v8
## @value v7

## @param {Version} version - Redis major version to deploy
version: v8

##
## @section TLS parameters
##

## @enum {string} AuthClients - Values accepted by the Redis `tls-auth-clients` directive.
## @value no
## @value optional
## @value yes

## @typedef {struct} TLS - TLS configuration.
## @field {*bool} [enabled] - Enable TLS for Redis and Sentinel connections. Disabled unless set. Enabling it moves Redis and Sentinel to a TLS-only listener, so existing plaintext clients must be migrated at the same time. Encryption is provided by the redis-operator fork that mounts the certificate Secret into both Redis and Sentinel pods at `/tls`.
## @field {AuthClients} [authClients] - Maps to the Redis `tls-auth-clients` directive. Defaults to `no` — the server certificate is presented but client certificates are not validated. `yes` requires a client certificate signed by this release's CA, which the platform does not issue. Changing it restarts the Redis and Sentinel pods, because the directive is read from the config file once at startup.

## @param {TLS} [tls] - TLS configuration. TLS is opt-in and is not inferred from `external`.
# Stays empty on purpose. Spelling out `authClients: "no"` here would make the
# generated README show the default instead of `{}`, but cozyvalues-gen quotes a
# default only for the literal types `string` and `quantity`; an `@enum` alias
# like AuthClients falls through to a YAML re-parse where `no` becomes boolean
# false. The schema would get `"default": false` on a string-typed enum property
# and the Go field `+kubebuilder:default:=false`. The default is resolved in
# templates/_tls.tpl instead, and
# hack/check-values-schema-string-defaults.bats fails on the coercion.
tls: {}

##
## @section Application-specific parameters
##

## @param {bool} authEnabled - Enable password generation.
authEnabled: true
